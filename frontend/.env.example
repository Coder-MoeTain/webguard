# Prefer leaving VITE_API_URL unset: requests use /api and Vite proxies to localhost:8001.
# Required when the UI is opened via LAN/public IP — do not point at http://localhost:8001
# from a non-localhost origin (browser Private Network Access block).
# Direct API (same host as browser): VITE_API_URL=http://YOUR_IP:8001
